@model Infozdrav.Web.Models.Trbovlje.AnalyserViewModel

@{
    ViewBag.Title = "Odstrani analizator";
    Layout = "_Layout";
}

<h2>@ViewBag.Title</h2>

<h3>Ali ste prepričani, da želite odstraniti:</h3>

<form asp-action="Remove" method="post">
    <div class="form-group">
        <input type="hidden" asp-for="Id" />
        <label asp-for="Name">Ime analizatorja</label>
        <input asp-for="Name" type="text" class="form-control" id="name" disabled>
    </div>
    <button type="submit" class="btn btn-primary">Odstrani</button>
    <a href="@Url.Action("Analyser")"
       class="btn btn-primary">Prekliči</a>
</form>
