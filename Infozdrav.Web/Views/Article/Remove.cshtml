@model Infozdrav.Web.Models.Trbovlje.ArticleFullViewModel

@{
    ViewBag.Title = "Odstrani sprejem artikla";
    Layout = "_Layout";
}

<h2>@ViewBag.Title</h2>

<h3>Ali ste prepričani, da želite odstraniti: </h3>

<form asp-action="Remove" method="post">
    <div class="form-group">
        <input type="hidden" asp-for="Id" />
        <label asp-for="Id">Article id</label>
        <input asp-for="Id" type="text" class="form-control" id="id" disabled>
    </div>
    <button type="submit" class="btn btn-primary">Odstrani</button>
    <a href="@Url.Action("Index")"
       class="btn btn-primary">Prekliči</a>
</form>
